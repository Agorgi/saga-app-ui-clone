// Wireframe clone: the production barrel also exports NotificationProvider and
// the useNotifications context from the notifications backend. The clone has no
// notifications backend, so only the UI surfaces are exported here.
export { NotificationDropdown } from './ui/NotificationDropdown/NotificationDropdown';
export { NotificationsPage } from './ui/NotificationsPage/NotificationsPage';
