/** Query params used when returning from Stripe poll donation checkout (`poll_checkout`). */
export const POLL_CHECKOUT_PAID_SEARCH_PARAM = 'poll_paid';
export const POLL_CHECKOUT_COMMISSION_SEARCH_PARAM = 'poll_commission';

/** Query params used when returning from Stripe standard commission pledge checkout. */
export const COMMISSION_CHECKOUT_PAID_PARAM = 'cc_paid';
export const COMMISSION_CHECKOUT_CANCELLED_PARAM = 'cc_cancelled';
export const COMMISSION_CHECKOUT_ID_PARAM = 'cc_id';
